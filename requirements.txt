# CD-Compliance-Checks orchestrator dependencies.
#
# The two external tools (Step 1 actigraphy-epoching, Step 2 actigraphy-sleep-
# metrics) are standalone script repos, not pip packages, so they are NOT listed
# here. After installing these deps, fetch them from GitHub with:
#     python setup_tools.py            # clone if missing
#     python setup_tools.py --update   # git pull the latest
# (that also installs each tool's own numpy/pandas/scipy/matplotlib requirements).

pyyaml>=6.0        # config loading
pandas>=1.5        # epoch CSV reading, compliance, Excel assembly
numpy>=1.23        # compliance statistics
openpyxl>=3.1      # .xlsx workbook writing

# Web dashboard (webapp/).
fastapi>=0.110     # dashboard API
uvicorn[standard]>=0.27   # ASGI server + websockets
